[project]
name = "pylabview-vi-xml-web"
version = "1.1.0"
description = "Docker-hosted UI for pylabview VI/RSRC conversion, component analysis, property editing and reconstruction"
requires-python = ">=3.11"
license = { text = "MIT" }

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-ra"

[tool.ruff]
target-version = "py311"
line-length = 110
extend-exclude = ["data"]

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B", "SIM"]
# FastAPI intentionally declares File/Form dependencies in endpoint defaults.
ignore = ["E501", "B008"]

[tool.ruff.lint.per-file-ignores]
# Third-party parsed type objects may expose descriptor methods that reject
# partially decoded payloads. Keeping the display string optional is intentional.
"app/lvkit_semantic.py" = ["SIM105"]
# This compatibility shim patches modules discovered at runtime, so static
# attribute assignment is intentionally unavailable. The try/except blocks
# keep older semantic-parser deployments usable when optional hooks are absent.
"app/deep_audit_runtime_patch.py" = ["B010", "SIM105", "UP035"]

[tool.mypy]
python_version = "3.11"
warn_unused_configs = true
warn_redundant_casts = true
warn_unused_ignores = true
no_implicit_optional = true
check_untyped_defs = true
exclude = ["data"]
